import React from 'react';

function Header() {
    return (
        <div className="Header">
            <h1>Pokemon Memory</h1>
            <p>Click on one of the Pokemon but do not pick it twice!</p>
        </div>
    );
}

export default Header;